# CMake toolchain for PS3

Just add the `bin` folder from the repository to the PATH variable, then run `ppu-cmake` or `spu-cmake`, or simply add `PS3Toolchain.cmake` as your CMake toolchain.

Before using, make sure the `PS3DEV` environment variable is set to the PS3 SDK location.
